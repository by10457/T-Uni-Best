---
trigger: glob
---
在你编写、修改代码时，已经代码完成后，我需要你进行如下操作
1.在编辑一个.ts .vue .js .jsx .html等等这类前端文件时，我需要你在函数、定义的变量、类、接口、类型、枚举上方，
  使用js的文档注释/** */编写注释，在函数内部的变量中使用// 在关键的逻辑或者变量的上面添加注释，
  在关键HTML标签上方使用<!-- -->添加注释
2.代码文件中，我希望有一个比较清晰自上而下的代码阅读结构，通常来说可能是导入模块、类型定义、变量定义、函数定义、
  函数调用、各项Vue生命周期、组件渲染...，需要你使用一个科学合理的顺序进行代码组织，
  比如：导入依赖 → 类型定义 → 变量声明 → 函数定义 → 组件选项 → 模板结构

这样可以提高代码的可读性和可维护性，方便团队协作和后续的代码审查。